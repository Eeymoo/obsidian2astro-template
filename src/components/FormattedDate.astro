---
interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time class="no-style no-underline" datetime={date?.toISOString()}>
  {
    date?.toLocaleDateString("en-us", {
      year: "numeric",
      month: "short",
      day: "numeric",
    })
  }
</time>
